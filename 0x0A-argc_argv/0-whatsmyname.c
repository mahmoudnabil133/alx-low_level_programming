#include "main.h"
#include <stdlib.h>
/**
 * main - playing with its arguments
 * @argc: the num of arg passed to main
 * @argv: array of string of args passed to main
 * Return: always 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0 * argc]);
	return (0);
}
