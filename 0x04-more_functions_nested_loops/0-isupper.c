#include "main.h"
/**
 * _isupper - check if the arg is upper or lower
 * @n: the char will b checked
 * Return: 0if lower 1 if upper
 */
int _isupper(char c)
{
	if (c >= a && c <= z)
		return (0);
	else
		return (1);
}
