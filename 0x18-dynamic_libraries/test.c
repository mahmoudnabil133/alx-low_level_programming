#include "main.h"
/**
 * add - adds 2 integers.
 * @a: a.
 * @b: b.
 * Return: an intiger.
*/
int add(int a, int b)
{
    return (a + b);
}
/**
 * sub - subs 2 ints
 * @a: a.
 * @b: b.
 * Return: an intiger.
*/
int sub(int a, int b)
{
    return (a - b);
}
/**
 * mul - mul 2 ints
 * @a: a.
 * @b: b.
 * Return: an intiger.
*/
int mul(int a, int b)
{
    return (a * b);
}
/**
 * div - div 2 ints
 * @a: a.
 * @b: b.
 * Return: an intiger.
*/
int div(int a, int b)
{
    return (a / b);
}
/**
 * mod - mod 2 ints
 * @a: a.
 * @b: b.
 * Return: an intiger.
*/
int mod(int a, int b)
{
    return (a % b);
}
