#include "main.h"
#include <string.h>
/**
 * _strcmp - it compares 2 str
 * @s1: str1
 * @s2: str2
 * Return: 0 if they are same less than 0 if s1<s2 bigger than 0 if s1>s2
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
