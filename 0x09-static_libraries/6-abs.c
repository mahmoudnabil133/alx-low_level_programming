#include "main.h"

/**
 * _abs - gets the absolute of the passed number
 *
 * @n : the number to get it,s abs
 *
 * Return: 0 Always
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (-n);
}
